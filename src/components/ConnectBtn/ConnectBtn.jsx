import React from "react";

const ConnectBtn = () => {
  return (
    <button
      style={{
        outline: "none",
        border: "none",
        padding: "0.5rem",
        fontWeight: "700",
      }}
    >
      <span>Connect Wallet</span>
    </button>
  );
};

export default ConnectBtn;
